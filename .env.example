# Optional absolute path to a private persistent volume. Never use public/.
# APPLICATIONS_STORAGE_DIR=/var/lib/techminds/applications

# Only enable behind a trusted proxy that overwrites X-Forwarded-For.
APPLICATIONS_TRUST_PROXY=false

